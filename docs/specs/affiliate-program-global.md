# 어필리에이트 프로그램 설계안 — 글로벌몰(biteme.one) 버전

> 원본: BITE ME JAPAN(biteme.co.jp) 어필리에이트 프로그램 설계안 개정 7 (2026-09-17, 외부 공유용)
> 이 문서는 원본의 기능 구조(링크 귀속·캠페인 기반 우대·정산 자동화)를 글로벌몰(biteme.one, `smart-paw-finder-global` 저장소) 인프라에 맞게 재작성한 기술 설계안이다.
> **운영 정책(커미션율·귀속창·환불처리·정산주기·부정행위·세무 등)은 JP를 그대로 따르지 않고 Shopify Collabs 정책과 비교해 별도로 정리했다 — [Global Affiliate 운영 정책 비교](affiliate-policy-global.md) 참고.**
> 작성 2026-09-18 · Issue #141

## 0. 원본과 달라지는 지점 — 요약

| 항목 | JP 원본 | 글로벌 버전 |
|---|---|---|
| 로그인 | LINE 로그인 | **Shopify Customer Account API OAuth** (이미 구현됨 — `src/lib/customer-auth.ts`, 이메일 기반 passwordless) |
| DB | Supabase | **Vercel KV** |
| 통화 | 엔화(¥) 고정 | 스토어 결제 통화(Shopify `currencyCode`) 기준 — 고정 통화 아님 |
| 계약 법인·세무 | 株式会社インケア, 일본 세법(源泉徴収·적격청구서·支払調書) | **미정 — 별도 법무/세무 확인 필요** (§7 참고) |
| 일정 | 실버위크 등 JP 전용 날짜 고정 | 날짜 제거, 단계(Phase) 구조만 유지 |
| 참고 모델 | 쿠팡 파트너스·지그재그 (국내 사례) | 동일 사례 유지, "국내" 표현만 일반화 |
| 현재 상태 | Shopify Collabs 승인제 운영 중 | **PR #110의 셀프 가입 + 관리자 수동 초대 + 전원 15% 고정 쿠폰** 구조를 걷어내고, 아래 §1의 셀프서비스형으로 전환 확정 |

## 결정 완료 — JP 방식(링크 기반)으로 전환

2026-09-18 확정. 아래는 그 결정을 반영한 설계안이다.

---

## 1. 현재 구조의 문제와 전환 방향

**기존(PR #110, 현재 라이브)의 문제:** 신청서를 제출해도 그걸로 끝이 아니다 — 관리자가 신청 내역(Google Sheets)을 확인하고 **수동으로 초대 이메일을 보내야** 파트너가 실제로 활동을 시작할 수 있다(`affiliateData.ts`의 "Get Invited — Receive your invitation by email" 단계). 승인 절차라고 부르진 않지만 실질적으로 사람이 한 명씩 처리해야 열리는 구조라, 파트너 수가 늘어날수록 운영 부담이 그대로 늘어난다. 게다가 초대와 함께 나가는 건 커미션 추적이 안 되는 **전원 동일 15% 할인 코드** 하나뿐이다.

**확정된 전환 방향:**
- 신청 완료 즉시 어필리에이트 계정 활성화 — **관리자 초대 단계 제거**
- 기본 어필리에이트는 **개인 추천 링크** 기반. 기본 할인 코드는 발급하지 않음
- 링크 클릭 → 고객 → 구매 → 커미션까지 서버에서 추적
- 할인 코드는 **운영자가 만든 캠페인에만** 선택적으로 발급 (JP 원안의 캠페인 구조 그대로)
- 파트너는 로그인 후 본인의 링크·전환·커미션 성과를 직접 확인

관리자가 신청자를 한 명씩 더 이상 손대지 않아도 되므로, 대규모로 확장 가능한 셀프서비스형 구조가 된다. 아래 §2 이후는 이 방향을 기준으로 작성했다.

---

## 2. 참여 구조 — 셀프 가입, 즉시 활성화

문은 활짝 열고, 우대만 골라서 얹는다는 원안의 원칙을 그대로 가져오되 "우대"의 기본값에서 관리자 개입을 완전히 뺀다.

| 항목 | 현재(PR #110) | 글로벌 버전 |
|---|---|---|
| 참여 | 신청 → **관리자가 Sheets 확인 후 수동 초대 이메일** → 그제서야 활동 가능 | 신청 즉시 활성화. 관리자 개입 없음 |
| 로그인 | 이메일 입력만 (계정 불필요) | **Shopify Customer Account 로그인 필수** — 귀속·중복가입 방지·"내 성과" 화면의 전제조건 |
| 발급물 | 전원 동일 15% 할인 코드 | 전원 전용 링크 `biteme.one/a/{code}`. **기본 할인 코드 없음** |
| 우대 | 없음 (전원 동일 코드) | 운영자가 만든 캠페인에만 선택적 할인 코드 (§3) |
| 데이터 | Google Sheets | KV 원장 (§6) |

### 가입 흐름
1. `/affiliate` 페이지에서 Shopify Customer Account 로그인 (미로그인 시 유도)
2. 로그인 후 신청 폼 — SNS 핸들·국가·유입경로(기존 PR #110 폼 재사용) + 18세 이상 확약·이용약관 동의 체크박스, 동의 버전·시각 기록
3. 제출 즉시 파트너 상태 `active`로 KV에 기록 — **초대 대기 없음**
4. 그 자리에서 전용 링크 `biteme.one/a/{code}` 발급, 복사 버튼 노출
5. "내 성과" 화면(`/partner`)으로 이동

기존 신청 폼 필드(소셜 계정+국가+유입경로)는 그대로 쓰고, "Get Invited" 단계와 관리자 수동 발송 절차만 제거한다.

---

## 3. 두 층 — 기본과 캠페인

| 층 | 대상 | 고객 할인 | 커미션 | 발급물 |
|---|---|---|---|---|
| 기본 | 가입한 전원 | 없음 | 제안 10% (§9에서 확정 필요) | 링크만 |
| 캠페인·지정 파트너 | 운영자가 고른 파트너 | 0~15% | 기본보다 높게 | 링크 + 전용 코드(할인 있을 때만) |
| 캠페인·전원/상품 | 기간·상품 한정, 전원 대상 | 없음 | +α | 없음 |

요율 해석 순서, 주문 시점 요율 고정, 커미션 기준액(할인 후 상품 소계, 배송비·세 제외) 등 계산 규칙은 원안과 동일하게 가져간다 — 통화만 스토어 결제 통화를 따른다(고정 ¥ 표기 없음).

캠페인이 도는 방식(관리자 화면에서 캠페인 생성 → 대상 지정 → 저장 시 코드 자동 발급 → 대상자 알림)도 원안과 동일. 다만 **LINE 푸시 대신 이메일 알림**(또는 마이페이지 인앱 알림)으로 대체.

---

## 4. 귀속 설계

원안의 3단계 귀속(코드 > 링크 > 고객)을 그대로 가져오되, 3번째 갈래의 정체성 신원 확인 수단만 바뀐다.

1. **코드 귀속** — 캠페인 전용 코드가 주문에 붙어 있으면 우선 (창 제한 없음)
2. **링크 귀속** — 파트너 링크 클릭이 30일 안에 있었으면 (창 기간은 Global 반품정책 확인 후 재검토 — JP는 7일 반품정책 기준 30일로 산출)
3. **고객 귀속** — 이 고객이 **Shopify Customer Account로 로그인한 상태**에서 30일 안에 누군가의 링크를 눌렀으면. 브라우저가 아닌 서버에 고객 ID 단위로 터치를 남기므로 기기를 바꾸거나 인스타/이메일 인앱 브라우저를 벗어나도 귀속이 따라온다.

링크가 주문까지 살아남는 경로(파트너 링크 → 클릭 기록(30일 저장) → 카트 속성에 파트너 참조 한 줄 → 주문 수신 시 판정 → 확정 대기 → 주문일+30일 후 확정)도 원안과 동일한 구조로 이식 가능. **카트 속성에 한 줄 추가하는 지점이 가장 위험한 변경**이라는 원안의 경고는 그대로 유효 — 결제 경로 변경은 반드시 단독 배포.

---

## 5. 화면

- **모집·가입 `/affiliate`** — 이미 존재(`src/pages/Affiliate.tsx`, `AffiliatePageContent.tsx`). 로그인 게이트 + 18세 확약·약관 동의 체크박스 추가.
- **파트너 성과 화면 `/partner`(신규)** — 링크 복사, 클릭→주문→확정→지급 퍼널, 이달 커미션, 정산 이력. 로그인한 Shopify 고객 세션 재사용.
- **관리자 「어필리에이트」 탭** — 파트너 목록(성과순), 캠페인 생성/관리, 정산(확정대기/확정/지급대기 → 명세 CSV), 이상 감지(자기구매·급증·신규 파트너 첫 주문).

---

## 6. 기록하는 것 (Vercel KV 원장)

Supabase 테이블 대신 KV 키 네임스페이스로 설계. 기존 `api/affiliate.ts`가 이미 쓰는 `affiliate:app:{id}`, `affiliate:email:{email}`, `affiliate:ids` 패턴을 확장한다.

| 원장 | KV 키 패턴(안) | 역할 |
|---|---|---|
| 파트너 | `affiliate:partner:{id}` | 코드·고객ID·SNS·약관 동의 버전/시각·18세 확약 |
| 캠페인 | `affiliate:campaign:{id}` | 이름·기간·커미션율·할인율·대상 |
| 캠페인 전용 코드 | `affiliate:campaign-code:{code}` | 캠페인 참조, 저장 시 생성·종료 시 비활성 |
| 클릭 | `affiliate:click:{id}` | 시각·랜딩·리퍼러·IP 해시·봇 여부 |
| 고객 터치 | `affiliate:touch:{customerId}` | 로그인 고객의 서버측 마지막 터치 |
| 전환 | `affiliate:order:{orderId}` | 귀속 방식·기준액·요율·커미션·상태·확정 예정일 (주문 1건=1행, 중복 방지) |
| 정산 | `affiliate:settlement:{period}:{partnerId}` | 총액·순액·상태·지급일·이의신청 마감일 |

개인 식별 정보 최소화 원칙(IP 해시만, 계좌정보는 초기 단계에 담지 않음)은 원안 그대로 유지.

---

## 7. 새는 곳과 통제

| # | 문제 | 대응 |
|---|---|---|
| 1 | 자기 링크로 자기 구매 | 주문 고객의 Shopify Customer ID·이메일이 파트너와 같으면 자기구매로 적재, 커미션 0. 결제는 막지 않음 |
| 2 | 계정 하나에 파트너 하나 | Shopify Customer Account 1인 1계정이 다계정 우회를 막는 가장 싼 방법 (LINE 대신 Shopify 로그인이 이 역할 수행) |
| 3 | 전용 코드 유출 | 고객당 1회 + 총 사용 횟수 상한 + 기간 종료. 기본 층이 코드를 아예 안 쓰므로(§1) 유출 위험은 캠페인 대상자로만 한정됨 |
| 4 | 링크 귀속 유실 | 서버측 고객 터치로 보강(§4 3번째 갈래) |
| 5 | 주문 데이터 중복·유실 | 주문 1건=1행, 일 1회 재대사 |
| 6 | 환불·취소 | 환불 알림 즉시 차감, 확정 직전 재확인 |
| 7 | 코드 충돌 | 현재 Global에 웰컴/증정 코드가 있는지 확인 필요 — 있다면 Shopify 주문당 코드 1개 제한과 충돌 가능 |
| 8 | 계좌 정보 | DB 평문 저장 금지, 초기 단계는 명세만 |
| 9 (신규) | **법무·세무 공백** | 계약 법인, 대상 국가별 소득세/원천징수, 이용약관 준거법 — 전부 미정. 구현 전 법무 확인 필수 |

---

## 8. 단계 계획 (날짜 미정 — Phase 구조만)

| Phase | 내용 | 비고 |
|---|---|---|
| 0. 장부 세우기 | 파트너 원장, 주문 수신 시 코드 매칭, 관리자 목록(읽기 전용) | 기존 PR #110의 KV 스키마 확장 |
| 1. 링크 귀속·확정·회수 | 파트너 링크 라우트, 클릭 기록, 카트 속성, 서버측 고객 터치, 확정/환불 처리 | 결제 경로 변경은 단독 배포 (CLAUDE.md 프론트 안전 규칙: `api/`, `vercel.json` Maintainer 확인 필수) |
| 2. 문 열기 | 로그인 게이트 추가, 관리자 수동 초대 제거·즉시 활성화 전환, 기본 할인 코드 발급 중단, 파트너 성과 화면 | 이미 라이브인 기존 가입 폼·기존 파트너 코드와의 마이그레이션 포함 |
| 3. 캠페인·정산 | 캠페인 등록, 전용 코드 발급, 월 마감·명세, 이상 감지 | 세무/법무 확인 완료 후 |

배포 3원칙(읽기→쓰기→노출 / 결제 경로 단독 배포 / 기존 주문 처리 절대 안 죽임)은 원안 그대로 가져온다.

---

## 9. 결정이 필요한 항목

| 항목 | 확인 필요 사항 |
|---|---|
| 기본 커미션율 | JP는 10% — Global도 동일하게 갈지 별도 확정 |
| 귀속 창·확정 대기 기간 | Global 실제 반품정책 확인 후 30일 유지 여부 결정 |
| 계약 법인·준거법·세무 | 어느 국가 법인/세율 기준으로 약관을 쓸지 |
| 코드 충돌 | 기존 웰컴/증정 코드 존재 여부 확인 |
| 통화 표기 | 커미션·정산 금액을 스토어 통화(currencyCode) 그대로 쓸지, 기준 통화(USD 등)로 환산해 관리자 화면에 통일 표시할지 |
| 기존 15% 코드 보유 파트너 마이그레이션 | 이미 발급된 전원 동일 코드를 언제·어떻게 회수/만료시킬지 (즉시 비활성 vs 유예기간) |

---

*이 문서는 코드 변경을 포함하지 않는다. §9의 남은 결정 사항을 확정한 뒤 별도 이슈/PR로 구현을 진행한다.*
